---
name: read_file
description: 读取文件内容
---

读取文件内容。

**参数**：
- `path` (必需): 文件绝对或相对路径
- `offset`: 起始行号（大文件分页用）
- `limit`: 读取行数（大文件分页用）

**使用场景**：
- 修改文件前必须先读取
- 了解文件内容和结构
- 大文件使用 offset/limit 分页读取

**注意**：
- 不要用 shell `cat` 代替
- 超过 500 行的文件建议分页
