---
name: grep
description: 在文件中搜索文本
---

在文件内容中搜索文本（支持正则表达式）。

**参数**：
- `pattern` (必需): 搜索模式（支持正则）
- `path`: 搜索目录或文件
- `include`: 文件过滤模式，如 `*.py`

**使用场景**：
- 查找函数/类/变量定义
- 查找特定字符串的使用位置
- 搜索 TODO、FIXME 等标记

**注意**：
- 优先于 shell `grep` 命令
- 正则特殊字符需要转义：`\(`, `\## `, `\.`
- 可以和 `glob` 配合缩小搜索范围
