---
name: write_file
description: 创建或覆盖文件
---

创建新文件或覆盖现有文件。

**参数**：
- `path` (必需): 文件路径
- `content` (必需): 文件完整内容

**使用场景**：
- 创建新文件
- 完全重写小文件
- 生成配置文件

**注意**：
- 优先用 `edit_file` 做小修改，而非覆盖
- 覆盖前确认是否需要保留原内容
- 不要用 shell `echo >` 代替
